to implement an lru cache class with o(1) average time complexity for get and put operations, i will use a combination of a hash map (dictionary) and a doubly linked list. the hash map will store keys mapped to nodes in the linked list, allowing for o(1) access to any key. the doubly linked list will maintain the order of usage, with the most recently used item at the head and the least recently used at the tail. when a key is accessed via get or updated via put, it will be moved to the head of the list. if the cache exceeds its capacity, the node at the tail will be removed from both the list and the hash map. for the linked list, i will define a node class with prev, next, and key-value fields. the cache will have a head and tail pointer to efficiently manage the list. the hash map will store references to the nodes. for the put operation, if the key already exists, the value will be updated and the node will be moved to the head. if the key does not exist and the cache is full, the least recently used node (tail) will be removed. for the get operation, if the key exists, the node will be moved to the head and its value returned; otherwise, -1 will be returned. to ensure efficient operations, i will need to handle the linked list updates carefully, including adjusting the pointers of the adjacent nodes when removing or moving a node. the capacity of the cache will be stored as an instance variable. if the capacity is zero or negative, the cache will be disabled. the implementation will need to handle edge cases such as when the cache is empty or when a key is accessed multiple times. the linked list operations, such as adding to the head, removing from the tail, and moving a node, must be done in o(1) time. the hash map will ensure that key lookups are o(1). if the capacity is not provided or is invalid, the cache will not be initialized. the implementation will not use any built-in ordered data structures, as per the requirements. the code will be written in a way that allows for easy modification and testing. the final implementation will be a class with the specified methods.