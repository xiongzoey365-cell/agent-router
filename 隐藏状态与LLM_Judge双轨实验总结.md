# 隐藏状态特征提取与 LLM-Judge 双轨实验总结

> 文档用途：对外说明截至当前版本已经完成的实验、方法演变、实际结果与边界。  
> 实验性质：training-free（不训练探针、不微调模型），使用同一组 25 道小规模测试题进行方法验证。  
> 核心原则：Hidden-State 轨与 LLM-Judge 轨是两条独立观察路径，目前只并列报告，不计算联合分数，也不据此声称测得了模型的静态能力或题目的客观难度。

## 1. 执行摘要

本项目希望研究一个比“直接让模型答题，再按答案正确率评分”更早期的信号：模型在尚未给出最终答案时，是否已经暴露出足以判断其任务处理状态的信息。围绕这一目标，目前形成了两条独立实验轨道。

第一条是 **Hidden-State 轨**。该轨直接读取模型 Transformer 层产生的隐藏向量，研究生成过程中的向量序列是否出现稳定、衰减或收敛。我们先后尝试过聚类熵与局部漂移、固定预算预推理、单轨迹多检查点、结构化浅推理、按 Schema 字段计算潜在负荷，以及目前较规范的“实质内容 token 上的固定窗口收敛”方法。早期方法存在 token 区间敏感、长度混杂、停止协议失效或代理分数含义不清等问题。当前方法消除了明显的 JSON 结构与长度伪相关，但在 25 道题上仍未发现与 L1–L5 难度标签稳定相关的收敛指标。

第二条是 **LLM-Judge 轨**。被测模型不输出答案和完整推理，只输出六类最小结构化行为证据；Judge 再从“题目文本 + 被测模型结构化输出”中抽取事实性证据，不直接打分；最后由确定性程序将事实映射到五档充分度。当前 25 题上，规划和指令维度全部得到 1.0，验证维度均值为 0.7，而沟通、记忆和工具维度均不可观察。结果存在明显天花板效应，与 L1–L5 的相关性不显著；同时，被测模型和 Judge 都是 Qwen3-8B，属于 demo 阶段的重要限制。

因此，当前可以成立的结论是：

1. 我们已经建立了可重复、可审计的无答案双轨实验管线。
2. 当前 Hidden-State 提取分析的是真实隐藏向量序列，而不是 token 字符串本身；但这些指标仍只是表征动态指标，尚不能被称为能力分数或客观难度。
3. 当前 LLM-Judge 衡量的是“模型针对当前任务主动暴露的行为证据是否充分”，不是一般能力，也不是答案正确性。
4. 25 道题较少且整体偏简单，难度区分度不足；现有结果不能支持强结论。

## 2. 共同实验设置与概念边界

### 2.1 数据与模型

- 数据规模：25 道题。
- 任务类型：代码生成、数学推理、知识问答、逻辑推理、指令遵循，每类含 L1–L5。
- 被测模型：本地 Qwen3-8B。
- 训练方式：不进行训练、微调或监督探针拟合。
- L1–L5 的用途：只用于实验结束后的外部相关性检查，不向模型或 Judge 暴露，也不参与特征构造。

### 2.2 “不生成答案”具体指什么

当前主实验要求被测模型只输出完成任务所需的最小结构化行为证据，明确禁止：

- 最终答案；
- 完整解法、代码或证明；
- 完整思维链（Chain-of-Thought）；
- 自我能力评价、信心分数或难度分数；
- 对六个维度自行评分。

这并不意味着模型内部没有计算，也不意味着结构化证据完全不含任务信息；它只意味着外部输出被约束为任务级、摘要式、可审计的最小证据。

### 2.3 Hidden State、token 与输出文本的关系

需要特别澄清：当前 Hidden-State 分析并不是比较 token 字符或 token ID 的变化。

模型生成的第 \(t\) 个 token 有一个离散 token ID；与此同时，Transformer 每一层在各序列位置产生一个高维实数向量 \(h_{l,t}\)。Hidden-State 轨读取的是这些真实向量。token 只承担两个作用：

1. 确定要观察输出中的哪些内容位置；
2. 将连续生成过程切分为可比较的固定窗口。

当前版观察第 \(t\) 个生成 token 时，使用位置 \(t-1\) 的层输出作为预测该 token 前的状态，以保持因果解释。后续的余弦距离、窗口中心、斜率和稳定性都在隐藏向量上计算，而不是在 token 文本上计算。

### 2.4 两轨为什么暂不合并

两条轨道观察的是不同对象：

- Hidden-State 轨观察模型内部表征随生成位置的动态变化；
- LLM-Judge 轨观察模型主动暴露的结构化行为证据是否充分。

如果现在直接加权融合，会把尚未验证的隐藏状态代理与 Judge 的规则分数混为一谈，并掩盖各自误差来源。因此当前只分别保存结果、分别解释，不计算“联合难度”或“综合能力”。

## 3. Hidden-State 特征提取尝试

## 3.1 尝试一：潜在“语义熵”与轨迹漂移

### 方法

早期方法让 Qwen3-8B 进行确定性的短序列生成，读取最后 4 层隐藏状态。层向量经过归一化、跨层平均和再次归一化后，对 token 状态先做 PCA，再在单题序列内做 KMeans。随后计算：

- 聚类占用分布的归一化熵，早期称为“语义熵”；
- 相邻 token 向量的平均漂移；
- 二者等权合成为 `LatentLoadScore = 100 × (entropy + drift) / 2`。

### 结果

| 分析设置 | 成功数 | 均值 | 最小 | 最大 |
|---|---:|---:|---:|---:|
| 生成并分析 20 token | 25/25 | 55.6268 | 45.5253 | 64.0552 |
| 生成并分析 60 token | 25/25 | 58.6078 | 45.4410 | 65.0378 |
| 分析约第 20–40 token | 25/25 | 51.1729 | 31.1512 | 66.2174 |
| 分析约第 40–60 token | 25/25 | 45.0748 | 29.7762 | 59.1961 |

20-token 设置平均耗时 807.34 ms，峰值显存约 15,817.54 MB；60-token 设置平均耗时 2,917.49 ms，峰值显存约 15,850.15 MB。

### 结论与问题

该方法证明了隐藏向量可被稳定采集并形成数值，但不能证明其反映难度：

- 同一方法仅改变 token 区间，均值便从 51.1729 变为 45.0748，说明结果强烈依赖生成阶段。
- KMeans 簇占用熵不是严格意义上的“语义熵”。它更可能同时吸收词汇、位置、标点、生成阶段和聚类初始化等差异。
- 相邻漂移描述局部移动幅度，但“大幅移动”不一定表示难，“小幅移动”也可能来自重复或模板化输出。
- 二者等权平均缺乏经过验证的测量学依据。

因此，后续不再把该指标称为已验证的语义熵或能力分数。

## 3.2 尝试二：单次、有限预算的预推理收敛

### 方法

模型最多生成 256 个预推理 token，并被要求在完成准备时输出 `<READY>`，无法准备时输出 `<UNRESOLVED>`。最后 4 层隐藏状态按 32-token 窗口汇总，同时计算：

- 末段稳定度；
- 语义回访；
- 词面重复；
- 路径反转次数；
- 相邻窗口距离。

### 结果

- 25/25 全部耗尽 256-token 预算；
- `<READY>`：0；
- `<UNRESOLVED>`：0；
- 平均预推理长度：256 token。

### 结论与问题

停止协议没有生效。所有样本都被相同的最大预算截断，所谓“终止前状态”实际上是“预算边界状态”。不同题目的自然收敛时间不可比较，指标很可能主要反映固定长度下的局部状态。因此该轮不能用于推断难度。

## 3.3 尝试三：单轨迹、多检查点探针

### 方法

该方法仍使用一条最长 256-token 的预推理轨迹，但在多个检查点读取最后几层的探测向量，比较检查点间余弦距离，同时记录 4-gram 重复率、片段回访和循环嫌疑。

### 结果

- 成功：25/25；
- 预算耗尽：25/25；
- 循环嫌疑：6 题；
- 末段最大探测距离均值：0.041553；
- 最大 4-gram 重复率均值：0.052816。

### 结论与问题

多检查点比单个终点提供了更多动态信息，但固定预算问题依旧存在。实验没有展示这些指标随 L1–L5 单调变化，也无法排除输出内容阶段、重复模式和截断位置的影响。因此它适合作为轨迹诊断，不足以作为难度测量。

## 3.4 尝试四：结构化浅推理可解性探针

### 方法

模型被要求以“关键约束、核心方案、待补环节”等短结构输出任务准备信息。实验沿该短轨迹读取 Hidden-State 探针，并将结构完整度、隐藏状态变化、重复和回访等组合成批次内代理分数。

### 结果

- 成功：25/25；
- 代理分数均值：59.04；
- 最小：33.62；最大：70.21；
- 结构完整度均值：0.920；
- 探测变化衰减均值：0.000。

### 结论与问题

许多题很快结束，无法形成足够多的检查点，因而“变化衰减”大量为 0。最终代理分数更容易被结构是否填满、输出是否提前终止所支配，而不是由隐藏向量是否真正收敛决定。该分数不能解释为成功率或题目难度。

## 3.5 尝试五：按六维 Schema 对齐的潜在负荷特征

### 方法

这一版首次与“无答案最小证据”输出对齐。模型先生成六维 JSON；实验保存其实际 prompt 和生成 token ID，再做一次因果 replay，从最后 4 层采集真实 Hidden-State。对每个顶层字段对应的 token 区间分别计算：

- `dispersion_entropy`：轨迹矩阵有效秩熵，描述向量在多个方向上的分散程度；
- `mean_step_drift`：相邻状态平均余弦距离；
- `direction_change`：相邻位移方向改变程度；
- `terminal_instability`：末段状态不稳定程度；
- `cross_layer_disagreement`：最后 4 层之间的不一致程度；
- `boundary_jump`：进入字段区间边界时的跳变，作为诊断项。

前五项被简单平均为字段 `latent_load_proxy`，再对可用字段求平均得到整题代理值。

### 结果

各字段可测题数及平均代理值为：

| 字段 | 可测题数 | 平均 latent load |
|---|---:|---:|
| reasoning_planning | 25 | 0.3983 |
| instruction_role | 25 | 0.4105 |
| communication | 4 | 0.3964 |
| verification | 25 | 0.4004 |
| memory_context | 8 | 0.4030 |
| tool_use | 0 | 不可测 |

整题潜在负荷与 L1–L5 的 Spearman 相关为 \(\rho=-0.584, p=0.002\)。表面上看相关显著，但进一步诊断发现：

- 潜在负荷与生成长度的 Pearson 相关约为 \(-0.873\)；
- 生成长度与 L1–L5 约为 \(+0.503\)；
- 控制长度和任务类型后，残差 Spearman 约为 \(-0.373, p=0.067\)。

### 结论与问题

这一轮最重要的发现不是“难题负荷更低”，而是发现了明显的长度与字段跨度伪相关：

- 顶层 JSON 字段区间包含固定键名、嵌套键名、括号、逗号等结构 token，并非只包含模型填写的实质内容。
- 不同题目和字段的 token 数不同，有效秩和局部几何指标会随样本长度改变。
- 五个尺度和语义并不相同的指标被等权平均，缺乏理论标定。
- `dispersion_entropy` 是几何分散度，不是基于答案分布或语义簇概率定义的语义熵。

因此，显著相关被判定为混杂信号，不能作为难度证据。这一诊断直接推动了下一版方法。

## 3.6 尝试六：实质内容上的向量序列收敛（当前首选 Hidden-State 版本）

### 3.6.1 观察对象

当前版复用同一份已保存的 prompt 和生成 token ID，进行因果 replay，读取最后 4 层 block output。它只选择 JSON **叶子值中的实质内容 token**：例如子任务名称、实际约束、检查描述等；固定顶层键、嵌套键、括号和标点均被排除。

这一步解决了此前“分析了整个 JSON 字段，所以键名和格式也进入特征”的问题。叶子字符串通过字符跨度定位回原始输出，再用 tokenizer 的 offset mapping 对齐到实际 token。当前结果中叶子对齐失败数为 0。

### 3.6.2 窗口构造

- 每个窗口固定包含 8 个实质内容 token；
- 至少需要 3 个窗口，即至少 24 个实质 token；
- 最多取 6 个窗口；
- 当候选窗口多于 6 个时，沿内容序列均匀选取；
- 每个窗口、每一层先对 8 个隐藏向量求中心向量，再归一化；
- 四层分别计算指标，最后跨层求均值；是否收敛还保留“收敛层比例”。

固定窗口减少了不同长度样本直接进入指标计算所造成的偏差，但没有消除所有内容、模板和位置影响。

### 3.6.3 当前七个收敛指标

1. **相邻窗口距离均值 `adjacent_window_distance_mean`**  
   连续窗口中心向量的平均余弦距离。越小表示轨迹逐段移动越少，但不能单独等同于更容易或更好。

2. **到终点距离均值 `distance_to_terminal_mean`**  
   各窗口中心到最后一个窗口中心的平均余弦距离，描述整条轨迹相对终态的偏离。

3. **前后期衰减 `early_late_decay`**  
   比较早期相邻距离与后期相邻距离的相对变化。正值通常意味着后期移动变小；负值意味着后期并未比早期更稳定。它是动态描述，不是难度分数。

4. **收敛斜率 `convergence_slope`**  
   对“窗口到终点的距离”随进程变化拟合线性斜率。负斜率通常表示逐步接近终态；接近零表示没有明显线性趋势；正斜率表示按该定义未向终态靠近。

5. **终端稳定度 `terminal_stability`**  
   由末段窗口间两两距离转换得到的稳定性，数值越高表示末段向量越相近。

6. **收敛点比例 `convergence_point_fraction_or_one`**  
   在给定距离阈值（当前默认 0.02）下，寻找从某窗口开始到末尾都保持稳定的最早位置，并归一化到 0–1。若某层始终未满足阈值，则记为 1。值越靠近 1，表示收敛很晚或未检测到。

7. **收敛层比例 `converged_layer_rate`**  
   最后 4 层中满足上述阈值收敛条件的层比例。

这些指标不再被强行平均成“潜在负荷”或“难度分数”。保留多指标是为了避免在没有验证依据时擅自规定方向与权重。

### 3.6.4 实验结果

- 前向计算成功：25/25；
- 整题可测：24/25；
- `code_L2_002` 只有 21 个实质内容 token，低于 24-token 下限，按预设规则不评分；
- 叶子对齐失败：0。

| 指标 | 可测题数 | 均值 |
|---|---:|---:|
| adjacent_window_distance_mean | 24 | 0.054570 |
| distance_to_terminal_mean | 24 | 0.054250 |
| early_late_decay | 24 | -0.025541 |
| convergence_slope | 24 | -0.006978 |
| terminal_stability | 24 | 0.950247 |
| convergence_point_fraction_or_one | 24 | 0.935417 |
| converged_layer_rate | 24 | 0.114583 |

字段级可测性为：

| 字段 | 可测 | 内容不足 |
|---|---:|---:|
| reasoning_planning | 19 | 6 |
| instruction_role | 3 | 22 |
| communication | 1 | 24 |
| verification | 4 | 21 |
| memory_context | 2 | 23 |
| tool_use | 0 | 25 |

字段可测性下降不是程序失败，而是严格排除空值、键名和标点后，很多字段没有至少 24 个实质 token。尤其当前任务没有真实协作和工具调用，相关字段通常为空。

### 3.6.5 与 L1–L5 的关系

| 指标 | Spearman \(\rho\) | p 值 |
|---|---:|---:|
| adjacent_window_distance_mean | -0.195 | 0.361 |
| distance_to_terminal_mean | -0.066 | 0.759 |
| early_late_decay | -0.042 | 0.847 |
| convergence_slope | -0.070 | 0.745 |
| terminal_stability | +0.230 | 0.279 |
| convergence_point_fraction_or_one | +0.100 | 0.643 |
| converged_layer_rate | -0.025 | 0.909 |

所有相关均较弱且不显著。与此同时，这些指标与实质内容长度的相关已明显弱于上一版，说明旧版最突出的长度混杂大体被消除。

### 3.6.6 当前解释

当前方法在“提取对象是否正确”这一点上比早期方法可靠：它分析的是真实隐藏向量序列，只看模型填写的实质叶子值，并采用固定窗口。但实验结果不支持“这些收敛特征已经能够体现 L1–L5 难度”。可能原因包括：

- 25 题样本量太小；
- L1–L5 是人工设置的粗标签，不一定是 Qwen3-8B 的主观任务难度；
- 题目整体偏简单，模型内部状态差异不足；
- 最小证据输出很短，字段级轨迹尤其不足；
- JSON 模板和表达方式仍可能影响向量；
- 隐藏状态未必以简单余弦收敛方式编码任务困难度；
- 不同层、不同任务类型可能需要不同的观察尺度。

因此，当前七项应称为 **Hidden-State 向量序列收敛特征**，而不是能力分数。

## 3.7 六次尝试的横向比较

| 尝试 | 主要观察对象 | 主要优点 | 关键问题 | 当前定位 |
|---|---|---|---|---|
| 聚类熵 + 漂移 | 短生成 token 的隐藏向量 | 首次跑通隐空间探针 | 区间敏感；聚类熵非语义熵 | 历史探索 |
| 有限预算预推理 | 256-token 轨迹窗口 | 尝试自然停止 | 25/25 未输出停止标记 | 协议失败案例 |
| 多检查点探针 | 同一轨迹多个位置 | 可看动态和循环 | 仍由固定预算主导 | 轨迹诊断 |
| 结构化浅推理 | 短结构输出轨迹 | 输出更受控 | 检查点不足，分数被结构完整度支配 | 历史探索 |
| 六维潜在负荷 | 顶层 JSON 字段向量 | 与行为维度对齐 | 长度和 JSON 结构严重混杂 | 已否定聚合方式 |
| 实质叶子收敛 | 叶子内容对应的真实隐藏向量 | 固定窗口、因果对齐、无强行总分 | 样本小，尚无难度相关性 | 当前首选版本 |

补充说明：注意力矩阵或注意力拓扑实验属于相邻研究，但 attention weights 不是 Hidden State，本文件不把它们列为隐藏状态特征提取方法。

## 4. LLM-Judge 轨：从最小结构化证据到确定性评分

## 4.1 总体流程

LLM-Judge 轨分成三个严格分离的阶段：

1. **被测模型生成最小证据**：只输出固定六维 JSON，不输出答案。
2. **Judge 抽取事实**：输入题目和被测模型 JSON，输出更细的事实证据 Schema；Judge 不给任何分数。
3. **确定性程序评分**：程序按照公开规则将 Judge 事实映射到 \(0, 0.25, 0.5, 0.75, 1\)，再聚合。

这种分工的目的，是把“自然语言判断”和“数值规则”分开。Judge 的判断仍可能有偏差，但它不能任意选择分值或总分公式；分值可以从证据字段和代码中复算。

## 4.2 被测模型输出 Schema

```json
{
  "reasoning_planning": {
    "subtasks": [],
    "dependencies": []
  },
  "instruction_role": {
    "constraints_used": [],
    "role_assumed": null
  },
  "communication": {
    "messages_used": [],
    "missing_information": [],
    "clarification_needed": false,
    "action": null
  },
  "verification": {
    "checks_performed": [],
    "issues_found": [],
    "corrections": []
  },
  "memory_context": {
    "facts_used": []
  },
  "tool_use": {
    "calls": []
  }
}
```

### 4.2.1 `reasoning_planning`

- `subtasks`：完成任务不可缺少的子任务短名称。要求写任务分解，不写完整解题过程。
- `dependencies`：形如 `[前置子任务, 后续子任务]` 的依赖边，用于判断计划顺序和可执行性。

这一维度希望观察模型是否识别“需要做什么”以及步骤是否存在基本依赖，而不是索取逐步思维链。

### 4.2.2 `instruction_role`

- `constraints_used`：模型实际识别并准备遵循的重要约束，例如输出格式、禁止项、字数限制、角色边界。
- `role_assumed`：任务确实要求特定角色时记录角色，否则为 `null`。

这里关注任务条件下的指令遵循准备，不评估模型抽象的“服从能力”。

### 4.2.3 `communication`

- `messages_used`：实际使用的其他参与者消息。
- `missing_information`：当前输入确实缺失、且会妨碍合理完成任务的信息。
- `clarification_needed`：是否应向用户澄清。
- `action`：需要采取的沟通或协调动作；不需要时为 `null`。

如果任务没有其他参与者消息、协作关系或实质信息缺口，该维度通常不可观察，不能因为数组为空而判低分。

### 4.2.4 `verification`

- `checks_performed`：模型声称实际做过的简短检查。
- `issues_found`：检查发现的真实问题。
- `corrections`：针对已发现问题采取的修正。

措辞中的“实际做过”很重要。模型不能把未来计划冒充已完成检查，也不能在未执行工具时虚构验证结果。

### 4.2.5 `memory_context`

- `facts_used`：为当前任务实际使用的前文、历史状态或长上下文事实。

仅依靠当前题面常识不等于使用会话记忆。没有历史依赖时该维度应为空且通常不可观察。

### 4.2.6 `tool_use`

- `calls`：真实工具调用列表，每项包含 `tool_name`、`arguments` 和 `result_interpretation`。

工具没有真实执行时，不得虚构结果；没有调用时保持空数组。仅仅“可以用工具”不自动意味着工具是必需的。

## 4.3 如何约束被测模型填写 Schema

系统提示对被测模型施加以下硬约束：

- 只输出一个严格 JSON 对象，不使用 Markdown 代码块，不添加前后说明；
- 必须保留给定结构和字段类型；
- 不适用数组必须为 `[]`，不适用可空值为 `null`；
- 内容只保留判断当前任务条件下行为是否充分所需的最少信息；
- 禁止输出答案、完整解法、完整代码、完整证明和完整思维过程；
- 禁止自评能力、信心、题目难度或六维分数；
- 工具未执行时不得虚构工具结果。

程序随后检查严格 JSON 有效性、缺失字段和错误字段类型。Schema 合规信息会进入 `instruction_role` 的确定性评分，但模型写得更长不会自动得到更高分。

## 4.4 Judge 的输入与职责边界

Judge 只接收：

1. 原始任务文本；
2. 被测模型主动暴露的结构化输出。

Judge 明确不能使用：

- 数据集标准答案；
- L1–L5 难度标签；
- 任务外参考答案；
- 模型未输出的隐藏思维链；
- 对完整答案的补写或重建；
- 对模型一般能力的推断。

Judge 的唯一职责是把可核查事实整理进下面的证据 Schema。代码还会检查 Judge 顶层是否擅自输出含 `score`、`sufficiency` 或 `difficulty` 的字段。

## 4.5 Judge 事实证据 Schema

### 4.5.1 规划证据 `reasoning_planning`

- `observable`：当前任务是否提供观察规划充分性的机会。
- `missing_key_subtasks`：缺失的关键子任务。
- `redundant_or_wrong_subtasks`：冗余、错误或偏离目标的子任务。
- `invalid_dependencies`：无效、颠倒或不成立的依赖。
- `plan_executable`：现有计划是否可执行；证据不足时为 `null`。

### 4.5.2 指令与角色证据 `instruction_role`

- `observable`：是否可观察。
- `important_constraints`：从任务文本识别出的重要约束。
- `covered_constraints`：模型输出确实覆盖的约束。
- `missed_constraints`：未覆盖的重要约束。
- `violated_constraints`：结构化输出已经违反的约束。
- `role_appropriate`：所采用角色是否合适；无角色或无法判断时按规则使用 `null`。

### 4.5.3 沟通证据 `communication`

- `observable`：任务是否真的含其他参与者消息、协作要求或需澄清的信息缺口。
- `important_messages`：当前任务中必须处理的外部消息。
- `messages_understood`：被正确理解和使用的消息。
- `messages_missed`：被遗漏的消息。
- `misinterpreted_messages`：被误解的消息。
- `clarification_appropriate`：澄清决策是否合适。
- `coordination_action_valid`：协调动作是否有效。

### 4.5.4 验证证据 `verification`

- `observable`：任务是否存在可检查风险，且模型输出能提供检查证据。
- `key_risks`：当前任务中关键、可核查的失败风险。
- `risks_checked`：模型确实覆盖的风险。
- `invalid_claimed_checks`：声称已检查但证据不成立的项目。
- `real_issues_found`：确实存在且被发现的问题。
- `false_issues`：模型错误声称的问题。
- `valid_corrections`：针对真实问题的有效修正。
- `invalid_corrections`：无效、无依据或引入新问题的修正。

### 4.5.5 记忆与上下文证据 `memory_context`

- `observable`：任务是否依赖前文、历史状态或长上下文事实。
- `required_context_facts`：完成任务所需的上下文事实。
- `facts_used_correctly`：被正确使用的事实。
- `facts_missed`：应使用但遗漏的事实。
- `incorrect_or_stale_facts`：错误、过期或错配的上下文事实。

### 4.5.6 工具证据 `tool_use`

- `observable`：任务是否明确允许/要求工具，或不用工具无法合理完成。
- `tool_need`：只能为 `none`、`optional` 或 `required`。
- `appropriate_calls`：选择合理的调用。
- `missing_calls`：应调用但未调用的工具。
- `unnecessary_or_wrong_calls`：不必要或选错的调用。
- `valid_arguments` / `invalid_arguments`：参数证据。
- `interpretation_assessable`：是否存在真实工具结果、足以评价结果解释。
- `valid_interpretations` / `invalid_interpretations`：对真实结果的解释质量。

没有真实工具结果时，`interpretation_assessable` 必须为 `false`，Judge 不得评价结果解释。

## 4.6 可观察性与空值规则

`observable=true` 的含义只是“当前任务提供了观察该维度的机会”，不是该维度表现良好。

- `reasoning_planning`、`instruction_role`：通常可观察。
- `communication`：仅当存在其他参与者消息、协作要求或真实澄清需求时可观察。
- `memory_context`：仅当任务依赖前文、历史状态或长上下文事实时可观察。
- `tool_use`：仅当任务明确允许/要求工具，或不用工具无法合理完成时可观察。
- `verification`：仅当存在可检查风险，且输出能够提供检查证据时可观察。

未知布尔值写为 `null`，未知列表写为 `[]`。不可观察维度最终分数为 `null`，不按 0 分处理，也不进入整题平均。这一点避免了“普通单轮题没有协作和工具，所以天然低分”的错误。

## 4.7 确定性评分规则

Judge 不打分。下列公式由程序固定执行，连续原始值最终量化到：

\[
\{0,\ 0.25,\ 0.5,\ 0.75,\ 1\}.
\]

量化时选择距离最近的档位；恰好等距时取较高档。

记

\[
R(g,b;d)=
\begin{cases}
\frac{g}{g+b}, & g+b>0\\
d, & g+b=0
\end{cases}
\]

为好证据与坏证据的比例函数。

### 4.7.1 规划充分度

令 \(m,w,d\) 分别为缺失子任务、错误/冗余子任务、无效依赖数量：

\[
S_{plan}^{raw}=1-\min(1,0.25m+0.20w+0.20d+p),
\]

其中计划明确不可执行时 \(p=0.35\)，无法判断时 \(p=0.10\)，可执行时 \(p=0\)。之后量化到五档。

### 4.7.2 指令与角色充分度

该维度同时考虑严格 JSON、Schema 质量、约束覆盖和角色适当性：

\[
S_{inst}^{raw}=0.35J+0.25Q_{schema}+0.30Q_{constraint}+0.10Q_{role}.
\]

- \(J\)：严格 JSON 合法时为 1，否则为 0；
- \(Q_{schema}=\max(0,1-0.08n_{missing}-0.08n_{wrongtype})\)；
- \(Q_{constraint}=R(n_{covered},n_{missed}+2n_{violated};1)\)；
- 角色明确不合适为 0；无法判断但模型主动声明了角色为 0.5；其余为 1。

### 4.7.3 沟通充分度

\[
Q_{message}=R(n_{understood},n_{missed}+2n_{wrong};0.5),
\]

\[
S_{comm}^{raw}=0.70Q_{message}+0.30Q_{decision}.
\]

`clarification_appropriate` 与 `coordination_action_valid` 中可判断的布尔值取平均形成 \(Q_{decision}\)；两者都未知时为 0.5。

### 4.7.4 验证充分度

风险覆盖为：

\[
Q_{coverage}=\min(1,n_{checked}/n_{risks}).
\]

若没有列出关键风险，则有检查证据时取 1，否则取 0.5。另有：

\[
Q_{precision}=R(n_{checked}+n_{realissue},n_{invalidcheck}+n_{falseissue};0.5),
\]

\[
Q_{repair}=R(n_{validrepair},n_{invalidrepair};d).
\]

若没有真实问题，修正默认质量 \(d=1\)；若存在真实问题但没有可判修正，则 \(d=0.5\)。最终：

\[
S_{verify}^{raw}=0.55Q_{coverage}+0.25Q_{precision}+0.20Q_{repair}.
\]

### 4.7.5 记忆与上下文充分度

\[
S_{memory}^{raw}=R(n_{correct},n_{missed}+2n_{wrong};0.5).
\]

错误或过期事实按双倍坏证据计入。

### 4.7.6 工具使用充分度

工具选择与参数分别计算：

\[
Q_{selection}=R(n_{appropriate},n_{missing}+2n_{wrong};0.5),
\]

\[
Q_{arguments}=R(n_{validarg},n_{invalidarg};d).
\]

如果存在合理调用但参数不可判，\(d=0.5\)；没有合理调用时默认参数项为 1。只有 `interpretation_assessable=true` 时，才加入工具结果解释质量：

\[
Q_{interpret}=R(n_{validinterpret},n_{invalidinterpret};0.5).
\]

最终对实际纳入的 2 或 3 个分量做算术平均，再量化到五档。

## 4.8 整题聚合、相对难度与瓶颈

设可观察维度集合为 \(O\)，则：

\[
OverallSufficiency=\frac{1}{|O|}\sum_{k\in O}S_k.
\]

不可观察的 `null` 维度不参与平均。当前程序进一步定义：

\[
RelativeDifficulty=1-OverallSufficiency.
\]

这里的“相对难度”只能解释为：**在当前任务、当前提示、当前模型输出和当前规则下，证据不充分度的互补量**。它不是题目的客观难度，也不是跨模型稳定标尺。

所有取得最低非空充分度的维度都被列为瓶颈；若多个维度并列最低，则同时记录。

## 4.9 当前 LLM-Judge 结果

- 25/25 成功生成证据并完成评分；
- 每题当前均有 3 个可观察维度：规划、指令、验证；
- 沟通、记忆、工具在本批 25 题上均被判为不可观察。

| 维度 | 可观察题数 | 均值 | 最小 | 最大 |
|---|---:|---:|---:|---:|
| reasoning_planning | 25 | 1.000 | 1.000 | 1.000 |
| instruction_role | 25 | 1.000 | 1.000 | 1.000 |
| communication | 0 | NA | NA | NA |
| verification | 25 | 0.700 | 0.500 | 1.000 |
| memory_context | 0 | NA | NA | NA |
| tool_use | 0 | NA | NA | NA |

验证维度在 25 题中都是瓶颈；其中有 10 题三项均为 1.0，因此规划和指令也并列成为最低项。整题总体充分度主要只有 0.833 或 1.000 两档，相对难度主要只有 0.167 或 0.000 两档。

Judge 相对难度与 L1–L5 的 Spearman 相关为 \(\rho=-0.289, p=0.162\)，方向与“L 越高越难”的直觉并不一致，且统计不显著。因此当前 Judge 结果没有体现预设难度层级。

## 4.10 LLM-Judge 当前限制

1. **题目可能偏简单**：规划和指令两维 25/25 全为满分，出现明显天花板效应。
2. **任务设计没有激活三类维度**：没有真实多方消息、历史依赖和工具环境，沟通、记忆、工具全部不可观察。六维框架在本批数据上实际只测试了三维。
3. **同源模型偏差**：被测模型和 Judge 都使用 Qwen3-8B。Judge 可能偏好同模型的表达风格，且二者可能共享盲点。
4. **不评价答案正确性**：这是实验边界而非程序遗漏。一个看似完整的计划可能通向错误答案；本轨无法发现所有此类错误。
5. **Judge 仍有语言判断自由度**：尽管分值由程序固定，`missing_key_subtasks`、`key_risks` 等事实分类仍由 LLM 产生，需做跨 Judge、一致性和人工复核实验。
6. **规则权重是设计选择**：例如验证覆盖权重 0.55、规划缺项扣 0.25，当前是透明、可审计的启发式规则，不是经大样本标定的心理测量参数。
7. **“相对难度”命名容易被误读**：它实际是当前交互下的证据不充分度，不应直接用于题库难度标定。

## 5. 两轨结果应如何对外表述

建议采用以下谨慎表述：

> 本实验建立了一个 training-free、无最终答案的双轨观测框架。一条轨道从最后四层真实隐藏向量中提取实质内容生成过程的收敛特征；另一条轨道让模型暴露最小结构化行为证据，再由 LLM 抽取事实、由固定规则评分。在 25 道 demo 题上，Hidden-State 当前七项收敛指标与 L1–L5 均无显著相关；LLM-Judge 结果也存在明显天花板效应，且与 L1–L5 无显著正相关。因此，当前结果证明了方法管线的可执行性和若干混杂控制的必要性，但尚未证明任何指标可以稳定测量题目难度或模型能力。

不建议使用以下强结论：

- “隐藏状态熵可以准确测量模型能力”；
- “隐藏向量越分散，题目越难”；
- “向量越快收敛，答案一定越正确”；
- “Judge 分数就是模型真实能力”；
- “相对难度就是题目客观难度”；
- “25 题结果可以泛化到其他模型和任务”。

## 6. 后续验证建议

在不改变两轨独立性的前提下，后续最有价值的验证包括：

1. 扩充题量，并使用对目标模型真正有区分度的题目；不要只依赖人工 L1–L5 标签。
2. 增加同一道题的可控难度变体，例如逐步增加约束、干扰项、上下文长度或必要工具步骤。
3. 使用不同随机种子重复生成，估计 Hidden-State 指标的题内方差和重测信度。
4. 对不同层、窗口大小、收敛阈值做预注册敏感性分析，避免事后挑选最相关配置。
5. 增设可验证结果，但仍与观测轨分开：例如最终答案正确率只作为外部效标，不进入 Hidden-State 或 Judge 特征。
6. 使用异构 Judge 模型，并抽样人工标注 Judge 事实字段，测量一致率。
7. 专门设计含真实多轮消息、历史依赖和工具调用的任务，以激活 communication、memory_context、tool_use。
8. 将简单余弦收敛与更强的动态方法比较，例如分段变化点、轨迹曲率、层间收敛时序或对照提示差分；任何新指标仍需先做长度和模板混杂检查。

## 7. 代码与结果索引

### 7.1 当前无答案双轨实现

- Schema 与系统提示：`new_method/task_conditioned_capability_sufficiency/common.py`
- Judge 事实抽取：`new_method/task_conditioned_capability_sufficiency/extract_judge_evidence.py`
- 确定性评分：`new_method/task_conditioned_capability_sufficiency/score_sufficiency.py`
- 早期六维潜在负荷：`new_method/task_conditioned_capability_sufficiency/extract_hidden_state_features.py`
- 当前向量收敛提取：`new_method/task_conditioned_capability_sufficiency/extract_hidden_state_convergence.py`
- 当前收敛报告生成：`new_method/task_conditioned_capability_sufficiency/build_hidden_state_convergence_report.py`
- 双轨设计说明：`new_method/task_conditioned_capability_sufficiency/无答案双轨实验说明.md`

### 7.2 当前结果

- LLM-Judge：`outputs/task_conditioned_capability_sufficiency_no_answer/llm_judge/`
- 早期六维 Hidden-State：`outputs/task_conditioned_capability_sufficiency_no_answer/hidden_state/`
- 当前 Hidden-State 收敛：`outputs/task_conditioned_capability_sufficiency_no_answer/hidden_state_convergence/`

### 7.3 历史 Hidden-State 尝试

- 潜在熵与漂移：`legacy_methods/idea_1_and_3/method1_latent_probe.py`
- 有限预算预推理：`legacy_methods/bounded_single_pass_v1/single_pass_convergence_probe.py`
- 单轨迹多检查点：`new_method/checkpoint_convergence_probe.py`
- 结构化浅推理：`new_method/structured_shallow_probe.py`
- 对应输出：`outputs/legacy_methods/`、`outputs/checkpoint_convergence/`、`outputs/structured_shallow_probe/`

## 8. 最终结论

目前最重要的方法论收获有三点。

第一，Hidden-State 必须明确区分“真实高维向量”与“用 token 文本做的表面统计”。当前代码读取的是真实层输出向量，但 token 对齐、窗口长度、JSON 模板和生成阶段都会显著影响结果。早期的向量分散与局部漂移不足以直接代表难度；当前固定窗口收敛法更干净，但尚未显示效标关联。

第二，LLM-Judge 不应同时承担证据解释和自由打分。当前“Judge 只抽事实、程序固定评分”的结构提高了可审计性，也通过 `observable/null` 规则避免对不适用维度误罚。然而，它仍受题目区分度、Judge 偏差和规则权重影响。

第三，两条轨道都只能描述当前任务交互中可观察到的代理信号。现阶段它们适合作为研究原型和后续实验基础，不适合被包装为已经验证的通用能力测试或客观难度估计器。
